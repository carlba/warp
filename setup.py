from setuptools import setup, find_packages


setup( name="warp",
      version='1.0.0',
      options = {},
      description='Standardlib',
      author='Carl Backstrom',
      packages=find_packages()
    #   install_requires=[
    #     "requirement >= version_number",
    # ],

)